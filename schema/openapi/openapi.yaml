openapi: '3.0.3'
info:
  title: Backbone API
  version: '1.0.0'
  description: |
    Auto-generated by backbone-cli.
    This API provides 11 standard CRUD endpoints for each model.

servers:
  - url: /
    description: Development server

tags:
  - name: Sysparams
    description: Sysparam management endpoints

paths:
  /api/v1/sysparams:
    get:
      tags:
        - Sysparams
      summary: List Sysparams
      description: Get a paginated list of Sysparams with optional filtering and sorting
      operationId: listSysparams
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
        - $ref: '#/components/parameters/SortByParam'
        - $ref: '#/components/parameters/SortOrderParam'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SysparamList'
        '401':
          $ref: '#/components/responses/Unauthorized'

    post:
      tags:
        - Sysparams
      summary: Create Sysparam
      description: Create a new Sysparam
      operationId: createSysparam
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateSysparamRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Sysparam'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /api/v1/sysparams/{id}:
    parameters:
      - $ref: '#/components/parameters/IdParam'

    get:
      tags:
        - Sysparams
      summary: Get Sysparam by ID
      operationId: getSysparam
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Sysparam'
        '404':
          $ref: '#/components/responses/NotFound'

    put:
      tags:
        - Sysparams
      summary: Update Sysparam (full)
      operationId: updateSysparam
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateSysparamRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Sysparam'
        '404':
          $ref: '#/components/responses/NotFound'

    patch:
      tags:
        - Sysparams
      summary: Update Sysparam (partial)
      operationId: patchSysparam
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchSysparamRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Sysparam'
        '404':
          $ref: '#/components/responses/NotFound'

    delete:
      tags:
        - Sysparams
      summary: Delete Sysparam (soft)
      operationId: deleteSysparam
      responses:
        '204':
          description: Deleted
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/sysparams/bulk:
    post:
      tags:
        - Sysparams
      summary: Bulk create Sysparams
      operationId: bulkCreateSysparams
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '#/components/schemas/CreateSysparamRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Sysparam'
    put:
      tags:
        - Sysparams
      summary: Bulk update Sysparams (full, atomic)
      operationId: bulkUpdateSysparams
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                allOf:
                  - type: object
                    required: [id]
                    properties:
                      id:
                        type: string
                  - $ref: '#/components/schemas/UpdateSysparamRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SysparamBulkResult'
        '400':
          $ref: '#/components/responses/BadRequest'
    patch:
      tags:
        - Sysparams
      summary: Bulk partial-update Sysparams (atomic)
      operationId: bulkPatchSysparams
      requestBody:
        required: true
        content:
          application/json:
            schema:
              oneOf:
                - type: object
                  required: [ids, patch]
                  properties:
                    ids:
                      type: array
                      items: { type: string }
                    patch:
                      type: object
                      additionalProperties: true
                - type: object
                  required: [items]
                  properties:
                    items:
                      type: array
                      items:
                        type: object
                        required: [id, patch]
                        properties:
                          id: { type: string }
                          patch: { type: object, additionalProperties: true }
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SysparamBulkResult'
        '400':
          $ref: '#/components/responses/BadRequest'

  /api/v1/sysparams/upsert:
    post:
      tags:
        - Sysparams
      summary: Upsert Sysparam
      operationId: upsertSysparam
      parameters:
        - name: key_field
          in: query
          schema:
            type: string
        - name: key_value
          in: query
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateSysparamRequest'
      responses:
        '200':
          description: Upserted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Sysparam'

  /api/v1/sysparams/trash:
    get:
      tags:
        - Sysparams
      summary: List deleted Sysparams
      operationId: listDeletedSysparams
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SysparamList'

  /api/v1/sysparams/{id}/restore:
    parameters:
      - $ref: '#/components/parameters/IdParam'
    post:
      tags:
        - Sysparams
      summary: Restore deleted Sysparam
      operationId: restoreSysparam
      responses:
        '200':
          description: Restored
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Sysparam'
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/sysparams/empty:
    delete:
      tags:
        - Sysparams
      summary: Empty trash for Sysparams
      operationId: emptyTrashSysparams
      responses:
        '200':
          description: Trash emptied
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted_count:
                    type: integer

  /api/v1/sysparams/delete/bulk:
    post:
      tags:
        - Sysparams
      summary: Bulk soft-delete Sysparams by ids (atomic)
      operationId: bulkDeleteSysparams
      requestBody:
        $ref: '#/components/requestBodies/BatchIds'
      responses:
        '200':
          description: Soft-deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  soft_deleted: { type: integer }
        '400':
          $ref: '#/components/responses/BadRequest'

  /api/v1/sysparams/restore/bulk:
    post:
      tags:
        - Sysparams
      summary: Bulk restore Sysparams by ids (atomic)
      operationId: bulkRestoreSysparams
      requestBody:
        $ref: '#/components/requestBodies/BatchIds'
      responses:
        '200':
          description: Restored
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SysparamBulkResult'
        '400':
          $ref: '#/components/responses/BadRequest'

  /api/v1/sysparams/restore/all:
    post:
      tags:
        - Sysparams
      summary: Restore all soft-deleted Sysparams
      operationId: restoreAllSysparams
      responses:
        '200':
          description: Restored
          content:
            application/json:
              schema:
                type: object
                properties:
                  restored: { type: integer }

  /api/v1/sysparams/trash/bulk:
    delete:
      tags:
        - Sysparams
      summary: Bulk permanently-delete Sysparams from trash by ids (atomic)
      operationId: bulkPermanentDeleteSysparams
      requestBody:
        $ref: '#/components/requestBodies/BatchIds'
      responses:
        '200':
          description: Permanently deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  permanently_deleted: { type: integer }
        '400':
          $ref: '#/components/responses/BadRequest'

components:
  schemas:
    Sysparam:
      type: object
      required:
        - id
        - group_name
        - key
        - value
        - data
        - sort_order
        - status
        - metadata
      properties:
        id:
          type: string
          format: uuid
        group_name:
          type: string
        key:
          type: string
        value:
          type: string
        data:
          type: object
        sort_order:
          type: integer
          format: int32
        status:
          type: string
        description:
          nullable: true
          type: string
        metadata:
          type: object

    SysparamBulkResult:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Sysparam'
        total: { type: integer }
        failed: { type: integer }
        errors:
          type: array
          items: { type: string }

    CreateSysparamRequest:
      type: object
      required:
        - group_name
        - key
        - value
        - data
        - sort_order
        - status
        - metadata
      properties:
        group_name:
          type: string
        key:
          type: string
        value:
          type: string
        data:
          type: object
        sort_order:
          type: integer
          format: int32
        status:
          type: string
        description:
          nullable: true
          type: string
        metadata:
          type: object

    UpdateSysparamRequest:
      type: object
      required:
        - group_name
        - key
        - value
        - data
        - sort_order
        - status
        - metadata
      properties:
        group_name:
          type: string
        key:
          type: string
        value:
          type: string
        data:
          type: object
        sort_order:
          type: integer
          format: int32
        status:
          type: string
        description:
          nullable: true
          type: string
        metadata:
          type: object

    PatchSysparamRequest:
      type: object
      properties:
        group_name:
          type: string
        key:
          type: string
        value:
          type: string
        data:
          type: object
        sort_order:
          type: integer
          format: int32
        status:
          type: string
        description:
          nullable: true
          type: string
        metadata:
          type: object

    SysparamList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Sysparam'
        meta:
          $ref: '#/components/schemas/PaginationMeta'

    PaginationMeta:
      type: object
      properties:
        total:
          type: integer
          example: 100
        page:
          type: integer
          example: 1
        per_page:
          type: integer
          example: 20
        total_pages:
          type: integer
          example: 5

    Error:
      type: object
      required:
        - code
        - message
      properties:
        code:
          type: string
          example: VALIDATION_ERROR
        message:
          type: string
          example: Invalid input data
        details:
          type: object
          additionalProperties: true

  parameters:
    IdParam:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid

    PageParam:
      name: page
      in: query
      schema:
        type: integer
        minimum: 1
        default: 1

    PerPageParam:
      name: per_page
      in: query
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20

    SortByParam:
      name: sort_by
      in: query
      schema:
        type: string

    SortOrderParam:
      name: sort_order
      in: query
      schema:
        type: string
        enum: [asc, desc]
        default: asc

  responses:
    BadRequest:
      description: Bad request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

    Unauthorized:
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

  requestBodies:
    BatchIds:
      required: true
      content:
        application/json:
          schema:
            type: object
            required: [ids]
            properties:
              ids:
                type: array
                items: { type: string }

  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

security:
  - bearerAuth: []
