apiVersion: 1

providers:
  - name: backbone
    orgId: 1
    folder: Backbone
    type: file
    disableDeletion: false
    allowUiUpdates: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
